import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  statueIndicatorStyle: {
    width: 12,
    height: 12,
    top: 30,
    right: 0,
    borderColor: '#fff',
    borderWidth: 1,
    position: 'absolute',
  },
});
