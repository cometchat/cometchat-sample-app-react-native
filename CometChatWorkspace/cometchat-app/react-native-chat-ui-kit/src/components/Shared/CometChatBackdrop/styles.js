import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  backdropStyle: {
    zIndex: 1000,
    backgroundColor: '#000',
    opacity: 0.3,
    position: 'absolute',
    width: '100%',
    height: '100%',
    top: 0,
    left: 0,
  },
});
