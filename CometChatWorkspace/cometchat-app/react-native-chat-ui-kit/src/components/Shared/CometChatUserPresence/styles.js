import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  statueIndicatorStyle: {
    width: 8,
    height: 8,
    top: 25,
    right: -2,
    // float: 'right',
    position: 'absolute',
  },
});
